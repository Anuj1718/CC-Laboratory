print("Hello, World!")
print("This is a test file.")

#Copy the HTTPs link from the GitHub repository and go to cloud shell
# Clone the repository


# rm -rf test-repo # Remove the existing test-repo directory if it exists
# cd/ # Change to the home directory
# git clone HTTPS_LINK # Replace with the actual HTTPS link of your GitHub repository
# cd test-repo # Change to the cloned repository directory
# ls -l # List the contents of the directory
# git status # Check the status of the repository   
# python3 test.py # Run the test.py file
# git add test.py # Stage the changes

# https://github.com/Anuj1718/CC-Laboratory.git 


# Step 1: Go to home directory (optional but clean)
#cd ~

# Step 2: Remove existing repo folder if it exists (to avoid errors)
#rm -rf CC-Laboratory

# Step 3: Clone the GitHub repository using HTTPS
#git clone https://github.com/Anuj1718/CC-Laboratory.git

# Step 4: Navigate into the cloned repository
#cd CC-Laboratory

# Step 5: List files to confirm it's cloned
#ls -l

# Step 6: Check git status
#git status

# Step 7: Run the Python file
#python3 test.py

# Step 8: If you made changes to the file, stage it
#git add test.py

# Step 9 (optional): Commit and push
#git commit -m "Updated test.py"
#git push origin main  # or `master` if your repo uses that
